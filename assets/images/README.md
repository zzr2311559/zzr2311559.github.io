# 图片文件夹

以后可以把背景图、头像、文章配图放在这里。

当前首页横幅图是：

```text
assets/images/coffee-shoujo.jpg
```

如果想设置全站背景图，可以放入：

```text
assets/images/background.jpg
```

然后到 `styles.css` 里把 `--background-image: none;` 改成 `--background-image: url("assets/images/background.jpg");`。
